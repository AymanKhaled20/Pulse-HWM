# Pulse-HWM runtime dependencies
PySide6>=6.7,<7
pyqtgraph>=0.13.7
psutil>=5.9.0
httpx>=0.27.0
python-dotenv>=1.0.0

# Accounts / cloud sync — refresh tokens live in Windows Credential Manager
# via the keyring package (DPAPI-backed); no secrets in files
keyring>=24.0

# Update-system trust anchors — Ed25519 release-manifest verification
# (public keys are embedded in pulse_hwm/cloud/updates/trust.py)
PyNaCl>=1.5.0

# GPU (NVIDIA) — graceful N/A if not present
nvidia-ml-py>=12.535

# Hardware temps (in-process LibreHardwareMonitorLib bridge; MPL-2.0, see THIRD_PARTY.md)
pythonnet>=3.1.0

# Windows-specific
wmi>=1.5.1 ; sys_platform == "win32"
pywin32>=306 ; sys_platform == "win32"
winotify>=1.1.0 ; sys_platform == "win32"
