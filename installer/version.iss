; Single version source for Inno Setup.
; CI regenerates this file from pulse_hwm/__init__.py and FAILS the release
; build if tag != __version__, so the two places can never drift.
; Local ISCC builds use the committed copy.

#define AppVersion "1.2.0"
